# Birthdays

## What it does

Birthdays is a Flask application backed by SQLite. It provides a web form for storing birthdays and a page for displaying the saved records.

## Run

From this directory:

```bash
flask --app app run
```

Open the local URL printed by Flask, add birthday records through the form, and refresh the page to view the database-backed list.

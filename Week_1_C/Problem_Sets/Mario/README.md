# Mario

## What it does

`mario.c` prints a right-aligned pyramid of hash characters. The program validates the requested height before generating the rows.

## Compile

```bash
clang mario.c -o mario -lcs50
```

## Use

```bash
./mario
```

Enter a positive height when prompted.

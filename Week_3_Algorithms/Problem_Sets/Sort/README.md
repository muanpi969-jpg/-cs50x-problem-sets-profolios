# Sort

## What it does

The Sort exercise compares sorting algorithms by examining their running time on input data. This folder contains benchmark inputs and recorded answers for that analysis.

No standalone Sort implementation was present in the workspace.

## Build and use

There is no program to compile in the current folder. The text files can be reviewed alongside the official [Sort specification](https://cs50.harvard.edu/x/psets/3/).

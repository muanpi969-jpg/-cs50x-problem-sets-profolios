# Movies

## What it does

The Movies exercise uses SQL to explore a film database and answer questions about titles, ratings, cast, directors, and release years.

## Use

Open the supplied database in SQLite and run a numbered query:

```bash
sqlite3 movies.db
sqlite> .read 1.sql
```

There is no compilation step. The `LICENSE.txt` file is preserved with the query set.

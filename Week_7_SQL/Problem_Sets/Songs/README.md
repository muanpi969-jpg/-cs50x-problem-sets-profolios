# Songs

## What it does

The Songs exercise uses SQL queries to answer questions about a music database, including artists, tracks, genres, and popularity.

## Use

Open the database with SQLite and run an individual query file:

```bash
sqlite3 songs.db
sqlite> .read 1.sql
```

Repeat with the numbered query files as needed. There is no compilation step.

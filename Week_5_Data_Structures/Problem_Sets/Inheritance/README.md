# Inheritance

## What it does

`inheritance.c` models the inheritance of blood-type alleles across generations using dynamically allocated family trees.

## Compile and use

```bash
clang inheritance.c -o inheritance
./inheritance
```

The program generates a family tree and prints each person's alleles.
